liste=[1,2,3,4,5]
liste2=[]
def inversement(l):
    if len(l)>0:
        valeur1=l[0]
        l[0]=l[4]
        l[4]=valeur1
    else:
        print("la liste est vide")
    print(l)
inversement(liste)
inversement(liste2)
